version: 2

models:
  - name: dim_salesreason
    description: 'Tabela de cadastro de razao da venda'
    columns:
      - name: 'sk_motivo_venda'
        description: "Chave substituta da tabela de razao da venda dim_salesreason."
        tests:
          - unique
          - not_null

      - name: 'id_motivo_venda'
        description: "Chave substituta da tabela de razao da venda dim_salesreason."
        tests:
          - unique
          - not_null    
      
      - name: 'nome_motivo_venda'
        description: "Chave primária da tabela de razao da venda salesreason."
        tests:
          - not_null
      
      - name: 'id_tipo_motivo_venda'
        description: "Chave primária da tabela de razao da venda salesreason."
        tests:
          - not_null